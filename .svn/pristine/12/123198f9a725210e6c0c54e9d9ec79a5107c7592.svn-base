<template>
  <div class="app-container">
    <el-radio-group v-model="page" style="margin-bottom: 30px;">
      <el-radio-button label="planList">计划列表</el-radio-button>
      <el-radio-button label="carList">车辆排队</el-radio-button>
      <el-radio-button label="dumpList">卸料列表</el-radio-button>
      <el-radio-button label="tempList">出机口温度</el-radio-button>
    </el-radio-group>
    <div class="mixing-main">
      <plan-list v-if="isShow('planList')"></plan-list>
      <car-list v-if="isShow('carList')"></car-list>
      <dump-list v-if="isShow('dumpList')"></dump-list>
      <temp-list v-if="isShow('tempList')"></temp-list>
    </div>
  </div>
</template>

<script>
  import planList from './planList';
  import carList from './carList';
  import dumpList from './dumpList';
  import tempList from './tempList';

  export default {
    name: 'mixing',
    data() {
      return {
        page: 'planList'
      };
    },
    components: {
      planList,
      carList,
      dumpList,
      tempList
    },
    methods: {
      isShow(target) {
        return target === this.page;
      }
    }
  };
</script>

<style scoped>
  .mixing-container{
    margin-top: -15px;
  }
</style>


